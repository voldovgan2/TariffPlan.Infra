apiVersion: v1
kind: Service
metadata:
  name: "{{ .Chart.Name }}-service"
  namespace: {{ .Values.global.namespace }}
spec:
  selector:
    app: {{ .Chart.Name }}
  ports:
    - name: backend
      protocol: TCP
      port: {{ .Values.service.port }}